import style from './listContacts.module.css';

const ListContacts = ({}) => {
  return (
    <ul className={style.ullist}>
      <li className={style.list}>Denni</li>
    </ul>
  );
};

export default ListContacts;
